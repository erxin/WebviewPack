<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jbc.table">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<!--            android:label="@string/app_name_tiger"-->
    <application
        android:allowBackup="true"
        android:allowClearUserData="true"
        android:icon="${appLogo}"
        android:roundIcon="${appLogo}"
        android:label="${appLabel}"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:resizeableActivity="true">
<!--    横屏                android:screenOrientation="landscape"-->
<!--    竖屏                android:screenOrientation="fullSensor"-->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustResize"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data android:name="android.max_aspect" android:value="2.4" />
        <meta-data android:name="android.min_aspect" android:value="2.4" />
    </application>
</manifest>